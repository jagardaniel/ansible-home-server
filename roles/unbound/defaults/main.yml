---

unbound_interfaces:
  - 127.0.0.1

unbound_access_control:
  - 127.0.0.1 allow

unbound_remote_control: true
unbound_verbosity: 1
unbound_extended_statistics: false

unbound_forward_zones: []
# Example: Forward all queries to Cloudflare
# unbound_forward_zones:
#   - name: "."
#     forward_addresses:
#       - 1.1.1.1
#       - 1.0.0.1

unbound_local_zones: []
# Example: Static local zone
# unbound_local_zones:
#   - name: int.example.se.
#     type: static
#     records:
#       - name: "grafana"
#         type: "A"
#         value: "192.168.0.1"
#       - name: "prometheus"
#         type: "A"
#         value: "192.168.0.5"
