kind: GlobalDatasource
metadata:
  name: prometheus
spec:
  default: true
  plugin:
    kind: PrometheusDatasource
    spec:
      scrapeInterval: 60s
      proxy:
        kind: HTTPProxy
        spec:
          url: "http://192.168.0.1:9090"
